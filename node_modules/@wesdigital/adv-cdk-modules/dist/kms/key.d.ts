import { Key } from 'aws-cdk-lib/aws-kms';
import { Construct } from 'constructs';
import { DeploymentEnv } from '../wesdigital';
import { IPrincipal } from 'aws-cdk-lib/aws-iam';
export interface KMSKeyModuleProps {
    keyName: string;
    deploymentEnv: DeploymentEnv;
    identifier?: string;
}
export interface KMSKeyTargetProps {
    serviceName: string;
    targetName: string;
}
export declare class StandardKMSKey extends Construct {
    private _props;
    get key(): Key;
    private readonly _key;
    constructor(scope: Construct, id: string, props: KMSKeyModuleProps);
    addDynamoDBFor(principal: IPrincipal, target?: KMSKeyTargetProps): void;
    addSecretsManagerFor(principal: IPrincipal): void;
    addCloudwatchLogs(logGroupNames: string[]): void;
}
