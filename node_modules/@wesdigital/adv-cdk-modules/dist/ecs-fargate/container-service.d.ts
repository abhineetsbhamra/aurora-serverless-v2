import { Construct } from 'constructs';
import { FargateService } from 'aws-cdk-lib/aws-ecs';
import { Key } from 'aws-cdk-lib/aws-kms';
import { FargateAutoscalingProps, FargateModuleProps } from './props';
export interface ContainerServiceAutoscalingProps extends FargateAutoscalingProps {
    metric: 'CPU' | 'Memory';
}
export interface ContainerServiceModuleProps extends FargateModuleProps {
    autoscaling?: ContainerServiceAutoscalingProps;
}
export declare class ContainerService extends Construct {
    private readonly _kmsKey;
    private readonly _service;
    get service(): FargateService;
    get kmsKey(): Key;
    constructor(scope: Construct, id: string, props: ContainerServiceModuleProps);
}
