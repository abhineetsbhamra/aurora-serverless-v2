import { Construct } from 'constructs';
import { CfnApp } from 'aws-cdk-lib/aws-pinpoint';
export interface PinpointApplicationProps {
    createSMSChannel: boolean;
    pinpointApplicationName: string;
}
export declare class PinpointApplication extends Construct {
    get pinpoint(): CfnApp;
    private readonly _pinpoint;
    constructor(scope: Construct, id: string, props: PinpointApplicationProps);
}
