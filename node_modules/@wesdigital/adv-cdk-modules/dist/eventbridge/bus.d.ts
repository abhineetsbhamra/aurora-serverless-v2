import { Construct } from 'constructs';
import { EventBus } from 'aws-cdk-lib/aws-events';
import { DeploymentEnv } from '../wesdigital';
export interface ArchivedEventBridgeBusModuleProps {
    archiveRetentionInDays: number;
    busName: string;
    deploymentEnv: DeploymentEnv;
}
export declare class ArchivedEventBridgeBus extends Construct {
    get bus(): EventBus;
    private readonly _bus;
    constructor(scope: Construct, id: string, props: ArchivedEventBridgeBusModuleProps);
}
