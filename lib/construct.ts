#!/usr/bin/env node
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs'

export interface AuroraServerlessV2ConstructProps {
  }

class AuroraServerlessV2Construct extends Construct {
    constructor(scope: Construct, id: string, props: AuroraServerlessV2ConstructProps) {
      super(scope, id);

      const AuroraServerlessV2Stack = new 
    })
    }
}
